name: NRT EAGLE CI Pipeline (uwtools)

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review, labeled, unlabeled]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:

  cleanup:
    concurrency:
      group: nested-eagle-ci-cleanup
      cancel-in-progress: false
      queue: max
    name: Clean old conda installs
    if: >-
      ${{
        contains(github.event.pull_request.labels.*.name, 'eagle-ursa') &&
        !contains(github.event.pull_request.labels.*.name, 'ci-running')
      }}
    runs-on: ursa
    steps:
      - name: Check out code
        uses: actions/checkout@v4
      - name: Clean old conda installs
        run: .github/scripts/clean-old-conda-installs

  run_pipeline:
    concurrency:
      group: nested-eagle-ci-run-pipeline
      cancel-in-progress: false
      queue: max
    name: Run Nested EAGLE end-to-end on Ursa
    if: >-
      ${{
        contains(github.event.pull_request.labels.*.name, 'eagle-ursa') &&
        !contains(github.event.pull_request.labels.*.name, 'ci-running')
      }}
    runs-on: ursa
    timeout-minutes: 1440
    environment:
      name: eagle-ursa

    steps:

      - name: Add ci-running label
        continue-on-error: true
        uses: actions-ecosystem/action-add-labels@v1
        with:
          labels: ci-running
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Perform cleanup
        run: .github/scripts/cleanup

      - name: Checkout PR head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          fetch-depth: 0
          clean: false 

      - name: Gate on relevant changes
        id: gate
        uses: actions/github-script@v7
        with:
          script: |
            const pr = context.payload.pull_request;
            const { owner, repo } = context.repo;

            let page = 1;
            const per_page = 100;
            const files = [];
            while (true) {
              const resp = await github.rest.pulls.listFiles({
                owner, repo, pull_number: pr.number, per_page, page
              });
              files.push(...resp.data.map(f => f.filename));
              if (resp.data.length < per_page) break;
              page += 1;
            }

            const relevant = files.some(
              f => ["Makefile", "pyproject.toml", "setup"].includes(f)
                || f.startsWith(".github/scripts/")
                || f.startsWith(".github/workflows/")
                || f.startsWith("config/")
                || f.startsWith("eagle/")
                || f.startsWith("envs/")
            );
            core.info(`Changed files: ${files.length}`);
            core.info(`Relevant changes: ${relevant}`);
            core.setOutput("relevant", relevant ? "true" : "false");

      - name: Skip (no relevant changes)
        if: steps.gate.outputs.relevant != 'true'
        run: echo No relevant changes, skipping. && exit 0

      - name: Build env and run pipeline
        env:
          PR_NUM: ${{ github.event.pull_request.number }}
          SHA: ${{ github.event.pull_request.head.sha }}
          INSTALLDIR: conda
        run: .github/scripts/nested-eagle

  cleanup_label:
    name: Cleanup ci-running label
    runs-on: ubuntu-latest
    if: always()
    needs: [run_pipeline]
    steps:
      - name: Remove ci-running label
        continue-on-error: true
        uses: mondeja/remove-labels-gh-action@v2.0.0
        with:
          labels: ci-running
          token: ${{ secrets.GITHUB_TOKEN }}
